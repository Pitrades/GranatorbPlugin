name: xpSave
version: '${project.version}'
main: org.silvius.granatorb.Granatorb
api-version: 1.19
commands:
  granatorb:
    usage: /granatorb
    aliases: []
    description: gives the granaor item


permissions:
  xpSave.granatorb:
    description: Gives the commandSender the "granatorb" item

